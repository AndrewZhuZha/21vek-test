/**
 * Конфиг для GitHub Pages preview (testing-cursor): статика без Node backend.
 * Подключается CI при деплое preview; не использовать в production.
 */
window.PortalConfigLocal = {
    auth: {
        enabled: false
    }
};
